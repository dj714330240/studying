spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/studying
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    database: mysql #mysql数据库
    show-sql: true #是否打印sql语句日志
    hibernate:
      ddl-auto: create #启动的时候自动建表
